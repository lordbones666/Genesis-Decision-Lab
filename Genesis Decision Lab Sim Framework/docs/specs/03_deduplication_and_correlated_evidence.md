# Deduplication & Correlated Evidence

Cluster-first aggregation is mandatory:
- deterministic cluster keys and key versions
- provenance-first grouping to prevent duplicate facts from compounding
- cluster metadata is persisted in SEO rows for replay
