# Scenario Generation Spec

Scenarios are structured objects compiled from typed templates. A valid scenario must include deterministic identifiers, trigger sets, evidence links, state and regime assumptions, simulator targets, and confidence metadata.

Compilation is deterministic from template id, label, and sorted evidence ids.
