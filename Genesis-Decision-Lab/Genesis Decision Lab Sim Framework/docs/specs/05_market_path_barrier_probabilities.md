# Market Path / Barrier Probabilities

Monte Carlo module provides seeded path crossing estimates with deterministic reproducibility checks.
Use as an optional expert module and avoid double counting shared market inputs with baseline signals.
